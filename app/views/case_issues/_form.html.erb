
  <p>Subject: <%= f.text_field :subject %></p>
  
  <p>
    Group Discussion:
    <%= f.radio_button :group_discuss, 'yes', {:checked => true} %>yes
    <%= f.radio_button :group_discuss, 'no' %>no
  </p>
    <%= f.hidden_field :user_id, :value => @current_user.id %>
    <%= f.hidden_field :case_repostory_id, :value => Course.find(params[:context_id]).case_repostory.id %>
  Template:
  <% if  @case_issue.case_tpl %>
    <h3>Warning: Your account don't has a case templates, so using default case template to create an issue.</h3>

    <div><button id="new-module"><%= t '', 'New Module' %></button></div>

    <%= render :partial => 'case_tpls/shared_case_tpl_form', :locals => {:case_tpl => @case_issue.case_tpl} %>

  <% else %>
    <div>
      <span>Please elect a case template:</span>
      <%= select_tag 'tpl', options_for_select(Course.find(params[:context_id]).root_account.case_tpls.map{|t| [t.name, t.id ]}.insert(0, 'Please select').push(['I want to create case template with myself.', '-1'])) %>
    </div>
  <% end %>

