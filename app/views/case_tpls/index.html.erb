<p>
  <% link_to new_account_case_tpl_path, {:class => 'btn btn-success'} do %>
    <i class='icon-add'></i>
    <%=  t('new_tpl', 'New Case Template') %>
  <% end %>
</p>
<% unless @case_tpls.empty? %>
  <table class="table table-striped table-bordered">
    <tr>
      <th><%= t 'headings.name', 'Name' %></th>
      <th><%= t 'headings.author', 'Author' %></th>
      <th><%= t 'headings.updated_at', 'Updated At' %></th>
      <th><%= t 'headings.op', 'Operation' %></th>
    </tr>

  <% @case_tpls.each do |case_tpl| %>
    <tr>
      <td><%= link_to case_tpl.name, account_case_tpl_path(@account, case_tpl) %></td>
      <td><%= case_tpl.user.name %></td>
      <td><%= case_tpl.updated_at.to_s(:db) %></td>
      <td>
        <div class="admin-link-hover-area">
          <div class="admin-links">
            <button class="al-trigger ui-button"><span class="al-trigger-inner">Manage (will be accessibly hidden)</span></button>
            <ul class="al-options">
              <li>
                <% link_to edit_account_case_tpl_path(@account, case_tpl) do %>
                  <span class='ui-icon ui-icon-pencil'></span> <%= t '#buttons.edit', 'Edit' %>
                <% end %>
              </li>
              <li>
                <% link_to account_case_tpl_path(@account, case_tpl), {:confirm => t('#buttons.confirm', 'Are you sure?'), :method => :delete} do %>
                  <span class='ui-icon ui-icon-pencil'></span> <%= t '#buttons.delete', 'Delete' %>
                <% end %>
              </li>
            </ul>
          </div>
        </div>
      </td>
    </tr>
  <% end %>
  </table>
<% else %>
  <%= t 'empty_tpls', 'Empty Case Templates' %>
<% end %>

